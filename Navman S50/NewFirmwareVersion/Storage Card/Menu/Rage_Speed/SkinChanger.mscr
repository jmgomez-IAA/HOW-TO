
	RegWriteDWord("HKLM", "SOFTWARE\Menu", "Exit",0)
	kill("WolfNCU.exe")

	spath   = SystemPath("ScriptPath")
	spath   = SubStr(spath, 1, ReverseFind(spath, "\")) 
	fConfig = spath\"WolfNCU.ini"
	
	if (IniRead(fConfig,"Skin","SkinName") eq "Skin_Blue")

		Iniwrite(fConfig,"Skin","SkinName","Skin_Red")

	else

		Iniwrite(fConfig,"Skin","SkinName","Skin_Blue")

	endif

	run(spath\"menus.exe")
	