
	RegWriteDWord("HKLM", "SOFTWARE\Menu", "Exit",0)
	kill("WolfNCU.exe")

	spath   = SystemPath("ScriptPath")
	spath   = SubStr(spath, 1, ReverseFind(spath, "\")) 
	fConfig = spath\"WolfNCU.ini"
	
	if (IniRead(fConfig,"Skin","SkinName") eq "Metal Speed Red")

		Iniwrite(fConfig,"Skin","SkinName","Metal Speed Blue")

	else

		Iniwrite(fConfig,"Skin","SkinName","Metal Speed Red")

	endif

	run(spath\"menus.exe")
	