
	RegWriteDWord("HKLM", "SOFTWARE\Menu", "Exit",0)
	kill("WolfNCU.exe")

	spath   = SystemPath("ScriptPath")
	spath   = SubStr(spath, 1, ReverseFind(spath, "\")) 
	fConfig = spath\"WolfNCU.ini"
	
	if (IniRead(fConfig,"Skin","SkinName") eq "MetalCC_1")

		Iniwrite(fConfig,"Skin","SkinName","MetalCC_2")

	else

		Iniwrite(fConfig,"Skin","SkinName","MetalCC_1")

	endif

	run(spath\"menus.exe")
	