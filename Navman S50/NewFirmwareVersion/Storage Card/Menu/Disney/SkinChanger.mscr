
	RegWriteDWord("HKLM", "SOFTWARE\Menu", "Exit",0)
	kill("WolfNCU.exe")

	spath   = SystemPath("ScriptPath")
	spath   = SubStr(spath, 1, ReverseFind(spath, "\")) 
	fConfig = spath\"WolfNCU.ini"
	
	if (IniRead(fConfig,"Skin","SkinName") eq "Chica")

		Iniwrite(fConfig,"Skin","SkinName","Chico")

	else

		Iniwrite(fConfig,"Skin","SkinName","Chica")

	endif

	run(spath\"menus.exe")
	